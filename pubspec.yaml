
name: ismail_bms
description: A frontend Flutter app for BMS control

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  charts_flutter: ^0.12.0
  cupertino_icons: ^1.0.2
  lucide_icons: ^0.257.0
  fl_chart: ^0.64.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
